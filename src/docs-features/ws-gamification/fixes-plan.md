# План исправлений бизнес-логики WS-геймификации

Документ фиксирует расхождения между `business-logic.md` и реальной реализацией (`compute-gamification.ts`, миграции, `gamification-events.md`). Для каждого пункта — что не так, что нужно сделать.

---

## ~~1. Зелёный день: начислять +3 коина~~ ✅ ГОТОВО

Миграция `011` уже установила `coins = 3` в БД. Код скрипта читает коины из `gamification_event_types` динамически — транзакции создаются автоматически. Проблема была только в документации.

**Исправлено:**
- `gamification-events.md` — `green_day` перенесён из «информационных» в таблицу с начислениями (+3)
- `gamification-db.md` — аналогично

---

## ~~2. Стрик: уточнить логику календарных дней~~ ✅ ГОТОВО

**Исправлено:**
- `compute-gamification.ts` — добавлена функция `countAbsenceDays()`, подсчёт стрика изменён на `calendarDays - absenceDays`. Выходные увеличивают стрик, отсутствия — замораживают.
- `business-logic.md` — раздел «Правила стрика» обновлён: уточнена разница между выходными (тикают) и отсутствиями (замораживают), добавлена ссылка на щит стрика.

---

## ~~3. Щит стрика: добавить в business-logic.md~~ ✅ ГОТОВО

Добавлен раздел «Щит стрика ("Вторая жизнь")» в `business-logic.md`: grace period 24ч, стоимость 100 коинов, ограничения.

---

## 4. Мастер планирования: уже реализован в скрипте

**Проблема:** `master_planner` (+450 коинов за 10 последовательных L3 в бюджете) работает в скрипте (step 5), зарегистрирован в `gamification_event_types`, но не описан в `business-logic.md`.

Это самостоятельная механика, не связанная с достижениями (achievements) — достижения работают по накопительным порогам коинов (бейджи), а master_planner — по серии конкретных задач.

**Что сделать:**
- Добавить раздел «Мастер планирования» в `business-logic.md`:
  - 10 последовательных задач L3, закрытых в рамках бюджета → +450 коинов
  - Любая L3, закрытая с превышением бюджета → серия сбрасывается (`master_planner_reset`)
  - Серия циклическая: после получения бонуса счётчик продолжает, каждые следующие 10 → снова бонус

---

## ~~5. Праздники и рабочие переносы: добавить в business-logic.md~~ ✅ ГОТОВО

**Исправлено:**
- `business-logic.md` — раздел «Выходные» переименован в «Выходные и праздники», добавлены описания государственных праздников (`calendar_holidays`) и рабочих переносов (`calendar_workdays`)

---

## ~~6. Clawback L2: не реализован в коде~~ ✅ ГОТОВО

**Исправлено в `compute-gamification.ts`:**
- Добавлен шаг 4e — ревизия approved L2: проверяет actual_hours (прямые + дочерние L3) vs max_time, при превышении → `budget_revoked_l2`
- Все clawback-события (L3, L3 lead, L2) теперь берут сумму из оригинальной транзакции через `coins_override` — списание всегда равно начислению
- Добавлено поле `coins_override` в `NewEvent`, `createTransactions` использует его приоритетно над справочником

---

## 7. Таймтрекинг: окно для внесения отчётов

**Текущее поведение:** скрипт проверяет наличие записи в `ws_daily_reports` за вчера. Если sync подтянул отчёт до запуска compute-gamification — нарушения нет.

**Уточнение:** это ок — окно ~10 минут между sync и compute-gamification. Но нужно убедиться, что sync не подтягивает отчёты за даты раньше вчерашнего дня (иначе задним числом можно закрыть красный день за позавчера).

**Что проверить:**
- Как работает sync ws_daily_reports — подтягивает ли он отчёты только за вчера или за произвольный период?
- Если sync может подтянуть отчёт за позавчера → compute-gamification уже создал red_day с idempotency_key, так что повторного запуска не будет, но стрик уже сброшен — это корректное поведение

**Что сделать:**
- Уточнить в `business-logic.md`: у сотрудника есть время до момента синхронизации (утро следующего дня) чтобы заполнить отчёт за вчера

---

## 8. section_red: записывать всех нарушителей в details

**Проблема:** код берёт `firstViolation = violations[0]` и записывает в details только одного нарушителя (`compute-gamification.ts:193`). Если у L2-раздела несколько L3-задач с нарушениями динамики от разных исполнителей — тимлид видит только первого.

**Что сделать в `compute-gamification.ts`, функция `checkSections`:**
- Вместо `firstViolation = violations[0]` записывать массив `violations` целиком в details
- Структура details:
  ```json
  {
    "ws_task_id": "l2_id",
    "ws_task_name": "L2 name",
    "ws_project_id": "project_id",
    "violations": [
      { "violator_email": "...", "ws_task_id": "l3_id", "ws_task_name": "...", "ws_project_id": "..." },
      { "violator_email": "...", "ws_task_id": "l3_id", "ws_task_name": "...", "ws_project_id": "..." }
    ],
    "violation_type": "task_dynamics_violation"
  }
  ```
- Обновить фронтенд: компонент отображения red_reasons для section_red должен показывать список нарушителей, а не одного

---

## Не требуют исправлений

### Idempotency keys — расхождение форматов
Документация `gamification-events.md` указывает формат `budget_ok_l3_{ws_task_id}_{user_id}`, код генерирует `ws_budget_ok_l3_{taskId}`. Функционально не влияет — idempotency обеспечивается в обоих случаях. Нужно синхронизировать документацию с кодом, но это косметика.
