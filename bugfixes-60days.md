# Исправленные баги — последние 60 дней (апрель–июнь 2026)

Список основан на коммитах в `main` с 2026-04-23 по 2026-06-22.  
Анализировались сообщения коммитов **и** диффы кода.

**Итого: 49 багов в ~25 коммитах.**

---

## Инвалидация кэша (7)

1. **2026-06-11 · `824c8f4`** — `resolveAlarm`/`unresolveAlarm` вызывали `revalidatePath('/')` вместо `revalidateTag(\`alarms:${wsUserId}\`)` — кэш тревог не инвалидировался после изменения статуса.

2. **2026-06-11 · `e671c93`** — У вызовов `revalidateTag` отсутствовал обязательный второй аргумент — некорректная инвалидация кэша тревог.

3. **2026-05-28 · `df958d5`** — `purchaseProduct` не вызывал `revalidateTag(balanceTag(...))` — баланс не инвалидировался после покупки товара в магазине.

4. **2026-05-28 · `df958d5`** — `buyStreakShield` не инвалидировал кэш баланса после покупки щита.

5. **2026-05-28 · `df958d5`** — `sendGratitude` с `type === 'gift' && gift_source === 'balance'` не инвалидировал баланс.

6. **2026-05-28 · `df958d5`** — `getGratitudeRecipients` делал запрос к БД при каждом вызове без кэширования.

7. **2026-05-26 · `539cda7`** — `shop/actions.ts` импортировал несуществующий `updateTag` вместо `revalidateTag` — runtime-ошибка при смене курса.

---

## SQL и серверная логика (8)

8. **2026-06-12 · `de1b24b`** — SQL-view считал день со щитом как `delta=0`: щит защищал от сброса, но не давал +1 к счётчику стрика.

9. **2026-06-02 · `36a81f0`** — Revit materialized views не сбрасывались 1-го числа: верхняя граница `fn_minsk_today() - 1` возвращала последний день предыдущего месяца — топ показывал весь прошлый месяц.

10. **2026-05-20 · `eb434fa`** — Лента отдела брала данные из materialized views без отрицательных транзакций — WS-баланс показывался отрицательным.

11. **2026-05-05 · `86f6ea7`** — Поиск причины красного дня в `ws_daily_statuses` шёл по `event_date` напрямую, хотя триггер `trg_fix_ws_event_date` прибавляет +1 день при записи.

12. **2026-04-29 · `8160dbd`** — Скрипт `compute-gamification` без `PENDING_WINDOW_DAYS` одобрил 1 134 события за задачи 1.5-летней давности. Проведён откат начислений.

13. **2026-04-28 · `3a92012`** — Стрик хранился как статическое число и не обновлялся до следующего прогона VPS. Переведено на read-time расчёт через SQL-view.

14. **2026-04-27 · `4c5388b`** — Sick day не удалял старые записи при обновлении/удалении подзадачи — диапазон дат не обновлялся.

15. **2026-04-24 · `6e0fc6f`** — `process_gamification_event` не ограничивала списание при штрафах/отзывах — баланс уходил в отрицательное значение.

---

## Права доступа и авторизация (2)

16. **2026-06-05 · `7ee58a2`** — 10+ страниц `/admin/*` были доступны любому авторизованному пользователю без вызова `checkIsAdmin()`.

17. **2026-05-26 · `ae44ae3`** — middleware декодировал JWT через `atob()` без нормализации base64url → base64 (`-` → `+`, `_` → `/`) — вход в админку ломался на токенах с URL-safe символами.

---

## Динамические цены и курс (3)

18. **2026-05-28 · `df958d5`** — Цена щитов в справке бралась из устаревшего поля `price` вместо динамического расчёта `cost_byn × coefficient × курс`.

19. **2026-05-26 · `539cda7`** — `setCrystalRate` не пересчитывал `ticket_price` в активных лотереях при изменении курса.

20. **2026-05-13 · `15aed42`** — Цена щита на странице покупки бралась из устаревшего поля `price` вместо динамического расчёта.

---

## Фильтры, группировка и ранжирование (8)

21. **2026-06-22 · `228f65b`** — `getUsersSortedByBalance` игнорировала диапазон дат — секции «Группа риска» и «Самые богатые» всегда показывали баланс за всё время независимо от выбранного периода.

22. **2026-05-25 · `a5be4c3`** — `getLowBalanceUsers(betaOnly)` возвращала глобальный нижний 10%-срез до применения фильтра по группе — при фильтре «только дизайнеры» показывала аутсайдеров из всех групп.

23. **2026-05-19 · `19f7f2e`** — Pending-задачи в мастере планирования загружались только при активном фильтре «Ожидание»; при просмотре «Все события» они пропадали. Для L2 ошибочно запрашивались дедлайн-задачи.

24. **2026-05-29 · `6252c15`** — `user.team ?? 'Без команды'` не перехватывал пустую строку `""` — пустое значение команды отображалось как пустота.

25. **2026-05-29 · `6252c15`** — `teamName` получал `null` вместо `'Вне команд - {deptName}'` на странице достижений — группировка прогресса ломалась.

26. **2026-05-14 · `b6717d1`** — Фильтр `area === 'gratitude'` не включал подкатегории `gratitude_help`, `gratitude_quality`, `gratitude_mentoring`.

27. **2026-04-25 · `909d484`** — Лидерборд ранжировал по локальному индексу `idx + 1` вместо серверного ранга — при одинаковых очках участники получали разные места.

28. **2026-05-04 · `d278ca2`** — `DepartmentContest.tsx` применял `.map((d, i) => rank: i+1)` без учёта ничьих — равные показатели отделов давали разные позиции.

---

## Производительность (2)

29. **2026-06-11 · `824c8f4`** — `getDepartmentFeedData` использовал `.filter()` внутри `.map()` — подсчёт наград работал за O(n²). Заменено на `Map`-индекс.

30. **2026-05-29 · `6252c15`** — Поллинг баланса каждые 30 сек перегружал сервер. Увеличено до 5 минут.

---

## Чатбот и справка (6)

31. **2026-06-11 · `824c8f4`** — `getAllHelpArticles` и `getChatbotArticlesWithChunks` использовали `createSupabaseServerClient()` вместо `createSupabaseAdminClient()` — запросы ограничивались RLS.

32. **2026-06-09 · `57ac48a`** — `checkChatAvailability` возвращала `true` на любой HTTP-ответ включая 502/503 — чат отображался доступным при недоступном бэкенде.

33. **2026-06-08 · `4138e70`** — UI оставался в `isWaiting=true` без fallback при недоступном сервере. Добавлен таймаут 12 сек.

34. **2026-06-03 · `3bd2a67`** — Ссылки в админке вели на `/admin/help/{slug}/edit` вместо `/admin/chatbot/{slug}/edit` — редактирование статей не работало.

35. **2026-06-03 · `3bd2a67`** — Статьи папки `chatbot` отображались в разделе справки пользователей из-за отсутствия фильтрации по полю `folder`.

36. **2026-06-03 · `f9e0de0`** — `target="_blank"` применялся ко всем ссылкам в чатботе без различия внутренних и внешних URL.

---

## UI и рендер (8)

37. **2026-05-14 · `b6717d1`** — `AREA_UI['gratitude_help']` возвращал `undefined` — карточки наград `gratitude_*` рендерились с иконкой по умолчанию `revit`.

38. **2026-05-14 · `8e4ce3c`** — `emptyRankLabel="Нет 💎 за WS"` показывался при чистом балансе ≤ 0, хотя пользователь мог зарабатывать бриллианты, перекрытые штрафами.

39. **2026-05-13 · `f9698d5`** — `enrichTransaction` не обрабатывала типы `ach_gratitude_*` — транзакции за достижения по благодарностям показывали технический текст вместо читаемого описания.

40. **2026-05-13 · `c2d6cbf`** — В `TransactionFeed.tsx` покупки с отрицательной суммой получали серый цвет вместо красного `var(--apex-danger)`.

41. **2026-05-13 · `15aed42`** — Плашка «Вторая жизнь» на стрике не показывалась при hover.

42. **2026-05-07 · `5c68175`** — Главная страница использовала `w-[640px] shrink-0` вместо `flex-1 min-w-0` — лейаут ломался на широких экранах.

43. **2026-05-05 · `e8cf72e`** — Блок лидерборда с жёстким `max-h-[264px]` обрезал список некорректно.

44. **2026-04-28 · `248ca03`** — Тултип в календаре стриков показывал дату в ISO-формате `YYYY-MM-DD` вместо `ДД.ММ.ГГГГ`.

---

## React-хуки и состояние (3)

45. **2026-05-19 · `99d1c30`** — `useMemo` в `AwardsFilters` не пересчитывался при изменении `limit` — зависимость отсутствовала в массиве deps.

46. **2026-05-19 · `fa4c298`** — `TeamFeedTable` не передавал `monthLabel` в рендер — метка месяца не отображалась.

47. **2026-05-13 · `6ce05f6`** — Даты транзакций формировались через `new Date(tx.event_date + "T00:00:00")` без нормализации таймзон — в граничных случаях дата уезжала на день раньше/позже.

---

## TypeScript (1)

48. **2026-05-14 · `27da7c0`** — Поля `earned_with_coins`/`coins_available` в `GratitudeAchProgress` были обязательными, хотя бэкенд не гарантировал их наличие — ошибка компиляции.

---

## Бизнес-логика (1)

49. **2026-05-13 · `d1a23e7`** — В `ProductCard.tsx` условие `canAfford = balance >= product.price` не учитывало бесплатную квоту щитов (`freeLeft > 0`) — пользователи с квотой, но нулевым балансом, не могли активировать бесплатный щит.
